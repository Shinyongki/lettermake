# 작업지시서 #007
**제목**: 마크다운 → HWPX 변환 — "마음 채움, 숲으로의 초대" 양식 재현 (Phase A)
**작성**: Claude.ai (PM)
**대상**: Claude Code (로컬 `C:\Projects\문서작성` 작업 디렉터리)
**상태**: 신규
**선행 폐기**: 작업지시서 #006 (다른 코드베이스 가정으로 작성되어 무효)
**목표 산출물 형식**: HWPX (한글) 파일
**시각 모델**: `2026년 종사자 심리지원교육 실시 계획(안) — 마음 채움, 숲으로의 초대` PDF (산출 HWPX를 한글에서 열었을 때 이 PDF와 시각적으로 일치해야 함)
**검증 입력 파일**: 로컬 루트의 `2026_숲으로의초대_실시안내서.md`
**활성 레퍼런스**: `generate_supervision_plan_internal.py` (이 스크립트가 Python으로 호출하는 결과와 동일한 결과를 마크다운으로 만들 수 있게 하는 게 목표)

---

## 배경

`hwpx_generator/*` 패키지에는 `add_doc_title()` / `add_doc_purpose()` / `add_section_block()` / `add_sub_heading()` / `add_bullet1()` / `add_bullet2()` / `add_note()` / `add_table()` 등 PDF 양식을 만들기에 필요한 **고수준 API가 이미 구현되어 있음**. 다만 마크다운 파서(`hwpx_generator/parsers/markdown_parser.py`)가 그 API의 일부에만 매핑되어 있어, 마크다운만으로는 PDF 양식을 재현할 수 없음.

본 작업은 **기존 API를 마크다운 입력으로 노출**시키는 일에 한정한다. **신규 hwpx 요소나 디자인 자산은 추가하지 않는다.**

---

## 작업 목록

| 번호 | 작업 | 난이도 | 종류 |
|------|------|--------|------|
| 7-1 | frontmatter(`---` 블록) 파싱 + DocTitle 확장 (`title_line1`, `subtitle`) | 중 | 기능 |
| 7-2 | 섹션바 좌측 셀과 우측 셀의 로마숫자 중복 제거 | 하 | 버그 |
| 7-3 | 마크다운 `-` 슬롯 매핑 재설계 (1·2·3·4단계 들여쓰기) | 중 | 기능 |
| 7-4 | `※` 시작 라인 → `add_note()` 매핑 | 하 | 기능 |
| 7-5 | Phase A 검증 + 회귀 점검 | 하 | 검증 |

**진행 방식**: 7-1 → 7-2 → 7-3 → 7-4 순서로 진행. 각 단계 완료 시마다 변경 라인 요약 + 회귀 결과만 짧게 보고. **7-5 검증 단계에서 사용자가 결과 HWPX를 확인한 뒤 Phase B(작업지시서 #008) 진행 여부 결정.**

---

## 대상 파일 (수정 후보)

```
hwpx_generator/parsers/markdown_parser.py        (모든 작업)
hwpx_generator/elements/doc_title.py              (7-1: subtitle 필드 추가)
hwpx_generator/xml_builder.py                     (7-1: 부제 렌더링, 7-2: 로마숫자 자동 부여 확인)
2026_숲으로의초대_실시안내서.md                   (7-5: 검증용, frontmatter 추가)
```

**손대지 말 것**:
- `cli.py` (인터페이스 변경 없음)
- `hwpx_generator/elements/*` 중 doc_title.py 외 파일 (현 작업 범위 아님)
- `generate_*.py` 7개 스크립트 (외부 클라이언트, 회귀 점검 대상은 됨)

---

## 작업 7-1 — frontmatter 파싱 + DocTitle 확장

### 목표 입력 포맷

```markdown
---
title_line1: "경상남도 노인맞춤돌봄서비스 광역지원기관"
title: "2026년 종사자 심리지원교육 실시 계획(안)"
subtitle: "- 마음 채움, 숲으로의 초대 -"
purpose: "노인맞춤돌봄 종사자의 심리·정서적 소진을 예방하고 자연 치유를 통한 회복과 재충전의 기회를 제공하여 종사자 심리건강 증진 및 돌봄 서비스 품질 향상을 도모하고자 교육을 진행함."
---

## 관련 근거 및 필요성
- (재)경상남도사회서비스원 노인맞춤돌봄서비스-327(2025. 3. 10.)호
  - 2026 경상남도 노인맞춤돌봄서비스 종사자 교육지원사업 계획(안)
- 2025년 노인맞춤돌봄서비스 사업안내(역량강화교육, 심리지원교육)
...
```

### 구현 순서

#### (1) `hwpx_generator/elements/doc_title.py` — DocTitle 확장

현재: `DocTitle` 데이터클래스에 `text` 필드 하나뿐.

변경:
```python
@dataclass
class DocTitle:
    text: str = ""           # 메인 제목 (필수)
    line1: str = ""          # 표지 윗줄 (기관/소속, 선택)
    subtitle: str = ""       # 표지 아랫줄 부제 (선택)
```

`text`의 기존 동작은 유지(하위 호환). `line1` 비어있으면 기존과 동일하게 1줄 출력, 있으면 윗줄에 작게 표시. `subtitle` 비어있으면 미렌더링.

#### (2) `xml_builder.py` — DocTitle 렌더링 확장

현재 `add_doc_title()`은 `text` 한 줄(또는 `\n` 분리 2줄)을 렌더링. 변경 후:
- `line1`이 있으면 메인 제목 위에 **작은 폰트(예: 메인 14pt / 라인1 11pt)** 별도 단락
- `subtitle`이 있으면 메인 제목 아래에 **중간 폰트(예: 12pt)** + 가운데 정렬 단락

**중요**: 정확한 폰트 크기/스타일은 `generate_supervision_plan_internal.py`가 `add_doc_title("기관명\n메인제목")`로 호출했을 때 실제로 어떻게 렌더링되는지 먼저 확인하고, 그것과 시각적으로 일관되게 결정. 폰트 크기 결정에 모호함이 있으면 보고할 것.

#### (3) `markdown_parser.py` — frontmatter 파싱 추가

현재: frontmatter 처리 없음.

추가 동작:
1. 입력 첫 줄이 `---` 인 경우 다음 `---`까지 frontmatter 블록으로 인식
2. 각 줄을 `key: value` 또는 `key: "value"` 패턴으로 파싱 (외부 라이브러리 없이)
3. 인식 키: `title`, `title_line1`, `subtitle`, `purpose`
4. `title` → 본문 `# 제목` 자동 감지보다 **우선**
5. `purpose` → 본문 `목적:` 라인 자동 감지보다 **우선**
6. frontmatter 자체는 본문에서 제거(이중 처리 방지)
7. 알 수 없는 키는 무시 (경고 메시지만 stderr로 1회)

호출 매핑:
```python
doc.add_doc_title(text=fm["title"], line1=fm.get("title_line1",""), subtitle=fm.get("subtitle",""))
if fm.get("purpose"):
    doc.add_doc_purpose(fm["purpose"])
```

### 검증
- frontmatter 4개 키 모두 사용 → 표지 3줄(기관/제목/부제) + 목적 박스 정상
- frontmatter 미사용 → 기존 동작과 100% 동일 (회귀 없음)
- `title`만 지정 → 기존 1줄 제목과 동일
- frontmatter에 알 수 없는 키 → 무시, 변환 성공

---

## 작업 7-2 — 섹션바 로마숫자 중복 제거

### 현상
사용자가 `## Ⅰ. 추진 배경` 으로 작성하면 → 섹션바 좌측에는 `xml_builder.py:989` 의 `_ROMAN[idx]`로 자동 Ⅰ, 우측에는 마크다운 제목 그대로 "Ⅰ. 추진 배경" → **중복 표시**.

현재 `markdown_parser.py:154`:
```python
heading_text = re.sub(r'^\d+\.\s*', '', heading_text)   # 아라비아 숫자만 제거
```

### 수정
정규식을 로마숫자(전각 ⅠⅡⅢⅣⅤⅥⅦⅧⅨⅩ / 반각 IVX) + 아라비아 숫자 모두 제거하도록 확장:

```python
heading_text = re.sub(
    r'^([ⅠⅡⅢⅣⅤⅥⅦⅧⅨⅩ]+|[IVX]+|\d+)\s*[\.\)、:．]?\s+',
    '',
    heading_text,
).strip()
```

### 검증 (각 케이스가 모두 "추진 배경" 으로 정규화되어야 함)
| 입력 | 기대 |
|------|------|
| `## Ⅰ. 추진 배경` | 추진 배경 |
| `## Ⅰ 추진 배경` (구분기호 없음) | 추진 배경 |
| `## 1) 추진 배경` | 추진 배경 |
| `## 1. 추진 배경` | 추진 배경 |
| `## I. 추진 배경` (반각 I) | 추진 배경 |
| `## 추진 배경` (숫자 없음) | 추진 배경 (회귀 없음) |
| `## 2026년 사업` (선두가 진짜 숫자 텍스트) | **2026년 사업** (지우면 안 됨) → 정규식의 구분기호+공백 조건이 이를 보호해야 함. 확인할 것 |

마지막 케이스가 핵심 회귀 함정. `\s*[\.\)、:．]?\s+` 부분이 구분기호 또는 공백을 강제하므로 "2026년"의 "년"이 영문자가 아니라 한글이라 매칭 끊김 → OK. 그러나 `## 2026 사업` 같이 숫자+공백+한글이면 "2026"이 제거될 수 있음. 이 경우는 의도적으로 사용자가 회피해야 한다고 결정(주석으로 명시).

---

## 작업 7-3 — 마크다운 `-` 슬롯 매핑 재설계

### 현상
보고에 따르면 `markdown_parser.py:219` 부근에서 마크다운 `- 항목` 1단계가 `add_bullet1(○)`가 아니라 `add_bullet2(―)`로 가고 있음 → 1단계 ○ 슬롯에 마크다운으로 접근 불가.

PDF의 슬롯 시각 위계:
1. `m` 또는 □ (1단계 — 가장 큰 박스)
2. ○ (2단계)
3. ― (3단계 dash)
4. ∙ 또는 • (4단계)

### 수정 방침

들여쓰기 칸수 → API 호출 매핑을 다음과 같이 명확화:

| 들여쓰기 (raw 공백 수) | 매핑 |
|------|------|
| 0칸 (`- 항목`) | `add_bullet1(text)` → 1단계 ○ (또는 □ — 프리셋의 prefix 기호에 따름) |
| 2칸 (`  - 항목`) | `add_bullet2(text)` → 2단계 |
| 4칸 (`    - 항목`) | `add_bullet3(text)` 또는 dash 슬롯 — **API 존재 확인 필요** |
| 6칸 (`      - 항목`) | `add_bullet4(text)` 또는 dot 슬롯 — **API 존재 확인 필요** |

**중요 가드**: `add_bullet3` / `add_bullet4` 가 패키지에 없으면 그 부분은 **이번 범위에서 제외하고 보고**. 멋대로 신규 추가하지 말 것. Phase B 후보로 이관.

### 회귀 위험
파서 매핑을 바꾸면 **기존 마크다운 입력으로 변환한 결과의 슬롯 종류가 달라질 수 있음**. `generate_*.py` 7개 스크립트는 마크다운 파서를 안 쓰므로 영향 없음.

다음 회귀 점검 후 진행:
- `examples/` 내 기존 마크다운 샘플 변환 결과의 슬롯 종류가 의도와 일치하는지(또는 그 의도가 명시돼 있는지) 확인
- 기존 동작에 강한 의존성이 있어 보이면 멈추고 보고

### 검증
새 회귀 파일 `tests/regression_bullet_levels.md`:
```markdown
---
title: "회귀"
---

## 섹션

- 1단계
  - 2단계
    - 3단계
      - 4단계
- 1단계 복귀
```
결과: 1단계 ○ → 2단계 ― → 3단계 (해당 API 있으면) → 4단계 (해당 API 있으면) → 1단계 ○ 복귀.

---

## 작업 7-4 — `※` 주석 매핑

### 현상
파서 보고에서 `add_note()` 는 존재하나 마크다운에서 호출 경로 불명확. 마크다운 `※ 텍스트` 라인을 `add_note()` 로 매핑하는 분기 추가 또는 확인.

### 수정
`markdown_parser.py` 본문 라인 처리 루프에서, `-` 매칭보다 **먼저** `※` 시작 라인을 잡아 `doc.add_note(text)` 호출하도록 분기 추가. 들여쓰기는 무시(어디서나 ※는 주석 슬롯).

```python
m_note = re.match(r'^\s*※\s*(.+)$', line)
if m_note:
    doc.add_note(m_note.group(1).strip())
    continue
```

### 검증
```markdown
## 섹션
- 항목 1
※ 출처: 2025년 조사 결과
- 항목 2
```
출력: 항목 1 → ※ 주석 (별도 슬롯) → 항목 2.

---

## 작업 7-5 — Phase A 검증

### 검증 입력
**로컬 루트의 `2026_숲으로의초대_실시안내서.md`** 를 사용한다. 이 파일이 PDF의 원본 마크다운.

#### 사전 작업 — frontmatter 보강
원본 파일에 frontmatter 블록이 없을 가능성이 높음. 작업 순서:

1. 원본을 `2026_숲으로의초대_실시안내서.원본.md` 로 백업.
2. 원본 파일 상단에 frontmatter 블록 4줄 삽입 (실제 PDF 표지 내용 그대로):
   ```
   ---
   title_line1: "경상남도 노인맞춤돌봄서비스 광역지원기관"
   title: "2026년 종사자 심리지원교육 실시 계획(안)"
   subtitle: "- 마음 채움, 숲으로의 초대 -"
   purpose: "노인맞춤돌봄 종사자의 심리·정서적 소진을 예방하고 자연 치유를 통한 회복과 재충전의 기회를 제공하여 종사자 심리건강 증진 및 돌봄 서비스 품질 향상을 도모하고자 교육을 진행함."
   ---
   ```
3. 원본 본문에서 위 4요소(기관/제목/부제/목적)에 해당하는 줄이 이미 본문으로 존재하면 **중복 표시 방지를 위해 본문에서 제거**(또는 frontmatter 우선 정책으로 본문 무시).
4. 원본 본문 중 **5페이지 예산표, 3페이지 일정 매트릭스, 2페이지 하단 회색 박스**가 마크다운으로 표현돼 있다면 그대로 둠. 변환은 시도하되 깨지면 Phase B 대상으로 보고만.

### 변환 명령
```
python cli.py -i 2026_숲으로의초대_실시안내서.md -o 2026_숲으로의초대_실시안내서.hwpx
```

### 검증 항목
| # | 확인 사항 | 통과 기준 |
|---|----------|----------|
| V1 | 표지 윗줄 = "경상남도 ..." | 시각 일치 |
| V2 | 표지 메인 = "2026년 ..." | 시각 일치 |
| V3 | 표지 부제 = "- 마음 채움 -" | 시각 일치 |
| V4 | 본문 1페이지 상단 목적 박스 | 박스 + 텍스트 정상 |
| V5 | 섹션바 Ⅰ~ 자동 부여, 우측 텍스트 중복 없음 | "Ⅰ 관련 근거 및 필요성" |
| V6 | □/○/― 슬롯 들여쓰기 정확 | PDF 1페이지 Ⅰ 섹션과 시각 동등 |
| V7 | ※ 주석 별도 슬롯으로 표시 | PDF 1페이지 Ⅱ 섹션의 ※ 라인과 시각 동등 |
| V8 | 평탄 표 (3·4컬럼) | 헤더 배경 회색, 본문 흰색, 테두리 정상 |

### 회귀 점검

| 입력 | 기대 |
|------|------|
| `examples/` 내 기존 마크다운 샘플 | 회귀 없음 (frontmatter 미사용 시 기존 동작과 동일) |
| `python generate_supervision_plan_internal.py` 직접 실행 | 회귀 없음 (마크다운 경로 안 거치므로 영향 없을 것이지만, `DocTitle.line1`/`subtitle` 필드 추가로 인한 dataclass 기본값 비호환이 없는지만 점검) |

---

## Phase B 예고 (작업지시서 #008)

이번 작업에서 **건드리지 않음**. 다음 요건은 다음 작업으로 이관:
- 다단 헤더 표 (PDF 5페이지 예산표)
- 세로/가로 병합 셀 (PDF 3페이지 일정 매트릭스)
- 셀 내 줄바꿈 / 셀 내 불릿 리스트
- 인용 박스 / 회색 배경 박스 (PDF 2페이지 하단 요약 박스)
- `GovDocumentPreset` 의 폰트·여백·셀마진을 frontmatter 키로 노출 (`font`, `margin_*`, `cell_margin_*`)
- 강조 굵게 `**텍스트**` 지원 여부 확인 및 매핑

Phase A 결과를 사용자가 확인한 뒤 Phase B 진행 여부 결정.

---

## 주의사항

- 각 작업(7-1~7-5) 완료 시마다 변경 함수명 + 라인 범위만 짧게 보고. 전체 diff는 사용자가 요청할 때만.
- **DocTitle 폰트/스타일 결정에 모호함이 있으면 즉시 멈추고 보고.** 시각 디자인은 PM(Claude.ai) + 사용자 판단 영역.
- `add_bullet3` / `add_bullet4` 가 패키지에 없으면 7-3의 3·4단계는 보고만 하고 미구현. 신규 API 추가 금지.
- 이번 범위 외 리팩터링 욕구가 생겨도 메모만 남기고 보고에 포함. 자체 판단으로 손대지 말 것.
- frontmatter 값에 큰따옴표가 중첩된 경우는 지원 범위 외. 단순 문자열만.
- 기존 `generate_*.py` 7개는 손대지 않음(임시 클라이언트, 다음 단계에서 어떻게 정리할지 별도 결정).
