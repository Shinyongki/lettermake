# 작업지시서 #008
**제목**: Phase A.5 — 인라인 강조 + 들여쓰기 슬롯 + 빈 단락 정리
**작성**: Claude.ai (PM)
**대상**: Claude Code (로컬 `C:\Projects\문서작성`)
**상태**: 신규
**선행**: 작업지시서 #007 Phase A 결과(`examples/외부보고서_샘플.hwpx`) 검토 후속
**시각 모델**: `2026년 종사자 심리지원교육 ... 마음 채움, 숲으로의 초대` PDF
**검증 입력**: `examples/외부보고서_샘플.md` (#007에서 생성된 그것)

---

## 배경

#007 Phase A 산출 HWPX와 PDF 비교 결과, 다음 4개 항목에서 시각 차이가 큼:

1. `**bold**` 인라인 강조 33회 모두 별표 텍스트 그대로 출력 (Phase B 이관 항목이었으나 영향 과대로 끌어옴)
2. `  - 항목` 들여쓰기 슬롯이 plain text로 출력 — □/○/― 위계 손실
3. 빈 단락 과다 — 줄 사이 공백이 PDF보다 듬성
4. `**가. 추진 목적**` 등 소제목 패턴이 plain text

이번 작업으로 1·2·3을 처리하고 4는 진단만. 5(표 다단 헤더/병합 셀)와 인용 박스는 작업지시서 #009로 분리.

---

## 작업 목록

| 번호 | 작업 | 우선순위 | 종류 |
|------|------|----------|------|
| 8-1 | 진단 — examples/외부보고서_샘플.md 실제 내용 공유 + 슬롯 미매핑 원인 추적 | 1 | 분석 |
| 8-2 | 인라인 `**bold**` 강조 처리 (run 분할) | 2 | 기능 |
| 8-3 | 들여쓰기 `-` 슬롯 매핑 정상화 (8-1 진단 결과에 따라) | 3 | 버그 |
| 8-4 | 빈 단락 과다 출력 수정 | 4 | 버그 |
| 8-5 | 소제목 패턴(`**가./나.**` 또는 `가./나.`) 처리 방침 결정 — **구현 X, 보고만** | 5 | 결정 |
| 8-6 | 재변환 + V1~V8 재검증 | 6 | 검증 |

진행 방식: **8-1 보고를 먼저 받고 8-2~8-4를 PM이 다시 지시.** 8-1 단계에서 진단이 끝날 때까지 코드 수정 금지.

---

## 작업 8-1 — 진단 (코드 수정 금지)

### 8-1-a. 샘플 마크다운 전문 공유

`examples/외부보고서_샘플.md` 의 전체 내용을 보고 본문에 그대로 붙여 제공.
- frontmatter 포함 전문
- 줄 단위로 들여쓰기(공백) 보존
- 표 마크다운 그대로

### 8-1-b. 슬롯 미매핑 원인 추적

다음 가설을 하나씩 확인:

(A) **bullet 라인이 `**` 강조로 시작해서 정규식이 끊김** — `markdown_parser.py` 의 `_RE_BULLET1` / `_RE_BULLET2` 정규식이 `**` 으로 시작하는 라인을 어떻게 처리하는지 점검.

(B) **들여쓰기 공백이 탭이거나 비표준 공백** — 실제 샘플 파일을 `cat -A` 또는 `od -c`로 비공백 문자 확인.

(C) **bullet 호출이 일어났지만 prefix가 비어 있어 시각적으로 안 보이는 것** — `xml_builder.py` 에서 bullet1/2/3 렌더 시 prefix 출력 코드를 짚어보고, `GovDocumentPreset` 의 bullet prefix 값 확인.

(D) **bullet 호출이 일어났지만 텍스트에 `**` 가 포함되어 인라인 마크업이 안 풀려 raw로 보이는 것** (이 경우는 8-2와 동시 해결됨).

각 가설에 대해 한 줄 결론 보고 (`확인됨` / `해당 없음` / `불명`).

### 8-1-c. 빈 단락 출처 추적

`<hp:t>  </hp:t>` 같은 공백만 들어 있는 단락이 어디서 호출되는지 추적:
- 마크다운 빈 줄 → `add_empty_paragraph()` 호출인지
- `**가. 추진 목적**` 다음의 빈 줄이 매번 빈 단락이 되고 있는지
- 표 앞뒤에 자동 삽입되는 빈 단락인지

### 8-1-d. 표 품질 검증

생성된 HWPX의 표 7개가 PDF의 표(페이지 1·2·3·4·5)와 시각적으로 어떻게 일치/어긋나는지 짧게 평가:
- 헤더 행 배경 D9D9D9 적용 여부
- 컬럼 수 일치 여부
- 다단 헤더(예: PDF 5페이지 예산표)나 병합 셀이 필요한 표가 평탄 표로 들어갔는지

---

## 작업 8-2 ~ 8-5 — 8-1 보고 후 PM 재지시

8-1 진단 결과를 받은 뒤 PM이 다음 작업의 구체적 수정 위치(파일/라인/함수)를 명시한다. 8-1 단계에서는 **추측에 기반한 선제 수정 금지.**

### 8-2 — 인라인 `**bold**` 강조 처리 (가이드)

- 위치 후보: `markdown_parser.py` 의 본문 라인 처리 루프, 텍스트 → `add_paragraph()` / `add_bullet*()` 호출 직전 단계
- 변경 방향: 텍스트를 `**` 기준으로 분할 → 일반 run + bold run 교차 출력. `add_paragraph()` 가 단일 문자열만 받는다면, `add_paragraph_runs([("일반",False), ("강조",True), ...])` 같은 새 API를 추가하기 전에 **기존 API에 동등한 메서드가 있는지 먼저 확인**. 없으면 보고만 하고 멈춤(신규 API 도입은 PM 승인 필요).
- 회귀 위험: 기존 마크다운에서 `**` 가 진짜 별표 두 개를 의미하던 케이스가 있으면 의미 달라짐. `examples/` 회귀 점검 필요.

### 8-3 — 들여쓰기 `-` 슬롯 매핑 정상화

- 8-1-b 결론에 따라 결정. 추측 금지.
- (A)가 원인이면: 정규식을 `**` 강조 시작 라인도 슬롯으로 인식하도록 보강
- (B)가 원인이면: 탭/비표준 공백을 표준 공백으로 정규화 (이미 7-3에서 작업 의도가 있었음)
- (C)가 원인이면: GovDocumentPreset의 bullet prefix 확인 후 PM과 논의
- (D)가 원인이면: 8-2 완료 시 자동 해결 — 별도 작업 불필요

### 8-4 — 빈 단락 과다 출력 수정

- 8-1-c 결론에 따라 결정.
- 일반 원칙: **연속된 빈 단락은 1개로 축약.** 표 앞뒤의 자동 빈 단락은 그대로 유지(섹션 분리용).
- 마크다운의 단순 빈 줄은 단락 종료 신호로만 사용하고 빈 단락 객체를 만들지 않음.

### 8-5 — 소제목 패턴 처리 방침 (보고만)

`**가. 추진 목적**`, `가. 생활지원사 (당일형 소그룹 진행)` 같은 패턴을 어떻게 처리할지 3안 비교 보고:

| 안 | 방식 | 장단 |
|----|------|------|
| α | 그대로 두기 (8-2의 bold 처리만 적용) | 단순. 시각적으로 굵은 글자만 됨. PDF의 sub_heading 위계는 표현 못 함 |
| β | 마크다운 `### 소제목` 을 `add_sub_heading()` 으로 매핑 + 사용자가 소스 md 수정 | 표준 마크다운. 사용자 작업 필요 |
| γ | `**가./나./다./라. 텍스트**` 패턴 자동 감지 → `add_sub_heading()` 자동 호출 | 사용자 작업 없음. 휴리스틱 위험(오탐) |

비교 결과만 보고. 결정은 PM이.

---

## 작업 8-6 — 재검증

8-2~8-4 적용 후:

1. 동일 명령으로 변환: `python cli.py -i examples/외부보고서_샘플.md -o examples/외부보고서_샘플.hwpx`
2. 다음 자동 검증 (Bash):
   ```
   unzip -p examples/외부보고서_샘플.hwpx Contents/section0.xml > /tmp/s.xml
   echo "별표 텍스트 남은 개수:"; grep -oP '<hp:t[^>]*>[^<]*\*\*[^<]*</hp:t>' /tmp/s.xml | wc -l
   echo "공백만 든 hp:t:"; grep -oP '<hp:t[^>]*>\s+</hp:t>' /tmp/s.xml | wc -l
   echo "단락 개수:"; grep -c '<hp:p ' /tmp/s.xml
   echo "표 개수:"; grep -c '<hp:tbl' /tmp/s.xml
   ```
3. 결과 비교 — 이전(별표 33회, 공백 단락 다수) → 이후(별표 0회 기대, 공백 단락 감소).
4. 한글에서 열어서 PDF와 시각 비교는 사용자가 직접 수행. 통과 기준은 PM/사용자가 결정.

---

## 주의사항

- **8-1 단계가 끝나기 전 코드 수정 금지.** 추측으로 손대지 말 것.
- 8-2의 `**bold**` 처리에 신규 API(`add_paragraph_runs` 등)가 필요해 보이면 **멈추고 보고**. 신규 API 도입은 PM 승인 필요.
- 표 품질(다단 헤더/병합 셀) 개선은 이번 범위 외 — 작업지시서 #009(Phase B)로 이관.
- `examples/외부보고서_샘플.md` 수정은 8-3에서 정규식 정상화 후에도 슬롯이 안 잡히는 케이스에 한해 검토. 일단 파서가 책임지는 방향.
- `2026_숲으로의초대_실시안내서.md` (원본 내부결재 양식)는 손대지 않음.
