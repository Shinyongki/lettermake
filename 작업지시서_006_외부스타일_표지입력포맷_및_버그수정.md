# 작업지시서 #006
**제목**: 외부 스타일 — 표지 입력 포맷 확장 + 누적 버그 수정
**작성**: Claude.ai (PM)
**대상**: Claude Code
**상태**: 신규
**참고 자료**: `/root/.claude/uploads/ab19f4f2-8d78-4fda-b89b-91a7b93e5ea7/1aa003b1-_____________.pdf` (목표 산출물 예시 — 경상남도 노인맞춤돌봄 심리지원교육 계획안)

---

## 배경

외부 스타일로 PDF 같은 보고서를 만들려고 했더니 표지·섹션바·들여쓰기에서 의도하지 않은 결과가 반복적으로 나옴. PDF와 현재 코드를 비교한 결과 6개 항목의 문제가 확인됨. 이번 작업으로 **단순 버그 수정 + 표지 입력 포맷 확장**까지 처리. **표(다단 헤더/병합 셀/셀 내 줄바꿈) 개선은 작업지시서 #007로 분리.**

---

## 작업 목록

| 번호 | 작업 | 난이도 | 종류 |
|------|------|--------|------|
| 6-1 | 들여쓰기 `indent_offset` 누수 버그 수정 | 하 | 버그 |
| 6-2 | 섹션 제목 선두 로마숫자 자동 제거 | 하 | 버그 |
| 6-3 | 첫 섹션 앞 빈 단락 삽입 제거 | 하 | 버그 |
| 6-4 | 표 행 셀 수 정규화 (헤더 폭에 맞춰 패딩/절단) | 하 | 안정성 |
| 6-5 | 표지 입력 포맷 확장 — frontmatter `title_line2` / `subtitle` / `purpose` | 중 | 기능 |
| 6-6 | CLAUDE.md 작성 규칙 갱신 + 회귀 검증 | 하 | 문서 |

**진행 방식**: 6-1 → 6-2 → 6-3 → 6-4 까지 한 묶음으로 처리 후 1차 중간 보고. 그 다음 6-5 진행 후 2차 보고. 6-6은 사용자 승인 후 진행.

---

## 수정 대상 파일

```
gonggong_hwpxskills-main/gonggong_hwpxskills-main/scripts/dynamic_builder.py
CLAUDE.md                                                 (6-6에서만)
examples/external_report_sample.md                        (6-5에서 신규 생성)
```

---

## 작업 6-1 — 들여쓰기 `indent_offset` 누수 버그 수정

### 현상
`### 소제목` 다음에 오는 `-` 항목들은 `indent_offset = 2` 보정이 걸려 첫 단계부터 ○로 시작함. 정상 동작. 그러나 그 뒤 0칸 들여쓰기 `- 항목`이 다시 와도 보정값이 살아 있어 0칸이 **2칸으로 잘못 인식**되어 ○로 그려짐. 1단계 □로 되돌아가지 못함.

### 원인
**파일:** `dynamic_builder.py:392-399`
```python
else:
    # 1단계: square
    item = BulletItem(type="square", text=text)
    ...
    indent_offset = 0     # ← 여기에 도달해야 리셋
    current_section.items.append(item)
```
보정값을 더한 `indent`가 이미 `>= 2`로 분기되어 들어가 버려서 `else` 가지에 진입하지 못함 → 리셋 안 됨.

### 수정
`indent_offset` 적용 **이전의 raw 들여쓰기 칸 수**가 0이면 항상 리셋되도록 변경.

**`dynamic_builder.py:353-400` 의 매칭 블록을 다음 흐름으로 교체:**

```python
m = re.match(r"^( *)-\s+(.+)$", stripped)
if m:
    if current_section is None:
        current_section = Section(title="")
        doc.sections.append(current_section)

    raw_indent = len(m.group(1))           # 마크다운 원본 들여쓰기
    text = m.group(2).strip()

    # 원본이 0칸이면 indent_offset을 무조건 리셋
    if raw_indent == 0:
        indent_offset = 0

    indent = raw_indent + indent_offset

    # ... 이하 if/elif 분기는 기존 그대로 ...
```

`else` 가지 내부의 `indent_offset = 0` 줄은 제거(이미 위에서 처리됨).

### 검증
- `test_3depth.md` 회귀: 기존 결과와 동일해야 함.
- 신규 회귀 케이스를 `tests/regression_indent_reset.md`로 생성:
  ```markdown
  # 회귀
  ## Ⅰ. 섹션

  ### 소제목
  - 가
    - 나
  - 다
  ```
  결과는 `가=○`, `나=―`, `다=□`. (다는 `### 소제목` 보정이 끝난 뒤 0칸이므로 □로 되돌아와야 함)

---

## 작업 6-2 — 섹션 제목 선두 로마숫자 자동 제거

### 현상
CLAUDE.md 예시는 `## Ⅰ. 추진 배경` 으로 작성하라고 함. 그런데 섹션바는 좌측 셀에 `enumerate` 인덱스로 로마숫자(Ⅰ, Ⅱ, …)를 **자동 부여**하고, 우측 셀에 마크다운 제목을 그대로 넣어 → `[Ⅰ] Ⅰ. 추진 배경` 같이 **중복 표시**됨.

### 수정
`parse_markdown_sections()` 의 `## ` 매칭 직후, section_title 선두의 "Ⅰ./Ⅱ./…" 또는 "1./2./…" 패턴을 제거.

**`dynamic_builder.py:301-309` 의 분기를 다음과 같이 보강:**

```python
if re.match(r"^## ", stripped):
    section_title = stripped.lstrip("# ").strip()
    # 선두 로마숫자 또는 아라비아숫자 + 구분기호 제거
    section_title = re.sub(
        r"^([ⅠⅡⅢⅣⅤⅥⅦⅧⅨⅩ]+|[IVX]+|\d+)[\.\)、:．]\s*",
        "",
        section_title,
    ).strip()
    current_section = Section(title=section_title)
    ...
```

### 검증
- `## Ⅰ. 추진 배경` → 좌측 Ⅰ, 우측 "추진 배경" (중복 없음)
- `## 1) 개요` → 좌측 Ⅰ, 우측 "개요"
- `## 추진 배경` (로마숫자 없음) → 좌측 Ⅰ, 우측 "추진 배경" (회귀 없음)
- `## Ⅰ 관련 근거 및 필요성` (구분기호 없는 케이스) → 좌측 Ⅰ, 우측 "관련 근거 및 필요성"
  - **주의**: 구분기호 없이 공백만 있는 경우도 처리할 것. 정규식의 `[\.\)、:．]\s*`를 `[\.\)、:．\s]\s*` 로 확장하거나, 별도 분기 추가.

---

## 작업 6-3 — 첫 섹션 앞 빈 단락 삽입 제거

### 현상
**파일:** `dynamic_builder.py:737-742`
```python
for i, section in enumerate(doc.sections):
    roman = _ROMAN[i] if i < len(_ROMAN) else str(i + 1)
    if proto.empty_para is not None:
        nodes.append(copy.deepcopy(proto.empty_para))   # ← i==0에도 삽입
```
첫 섹션 앞에도 빈 단락이 들어가 목적 박스(또는 제목)와 Ⅰ 섹션바 사이가 비정상적으로 떨어짐.

### 수정
```python
for i, section in enumerate(doc.sections):
    roman = _ROMAN[i] if i < len(_ROMAN) else str(i + 1)
    if i > 0 and proto.empty_para is not None:
        nodes.append(copy.deepcopy(proto.empty_para))
```

### 검증
- 단일 섹션 마크다운 → 빈 단락 0개
- 3 섹션 마크다운 → 빈 단락 2개 (Ⅱ, Ⅲ 앞에만)

---

## 작업 6-4 — 표 행 셀 수 정규화

### 현상
**파일:** `dynamic_builder.py:334-340`
```python
cells = [c.strip() for c in stripped.strip().strip("|").split("|")]
if current_section.items and isinstance(current_section.items[-1], TableItem):
    current_section.items[-1].rows.append(cells)
```
헤더가 4칸인데 행이 3칸 또는 5칸으로 와도 그대로 누적 → `_build_table_element()` 에서 셀이 어긋난 XML 생성.

### 수정
행 추가 시점에 헤더 길이에 맞춰 패딩 또는 절단:

```python
cells = [c.strip() for c in stripped.strip().strip("|").split("|")]
if current_section.items and isinstance(current_section.items[-1], TableItem):
    last_tbl = current_section.items[-1]
    ncols = len(last_tbl.headers)
    if len(cells) < ncols:
        cells = cells + [""] * (ncols - len(cells))
    elif len(cells) > ncols:
        cells = cells[:ncols]
    last_tbl.rows.append(cells)
else:
    tbl = TableItem(headers=cells, rows=[])
    current_section.items.append(tbl)
```

### 검증
- 헤더 3칸 + 데이터 행 2칸 → 빈 셀 1개 자동 패딩
- 헤더 3칸 + 데이터 행 5칸 → 뒤 2칸 절단
- 정상 케이스(헤더와 동일 칸 수): 회귀 없음

---

## 작업 6-5 — 표지 입력 포맷 확장 (frontmatter)

### 배경
첨부 PDF 표지는 다음 4요소를 갖는데, 현재 마크다운으로는 표현 불가:

1. **기관/소속 라인** — "경상남도 노인맞춤돌봄서비스 광역지원기관"
2. **메인 제목 라인** — "2026년 종사자 심리지원교육 실시 계획(안)"
3. **부제** — "- 마음 채움, 숲으로의 초대 -" (`-`로 시작해서 현재 bullet으로 잘못 인식됨)
4. **목적 박스** — "노인맞춤돌봄 종사자의 심리·정서적 소진을 예방하고…" (`목적:` 접두어가 없는 자유 텍스트)

본문 라인 패턴 매칭으로 4가지를 구분하려는 현재 방식은 모호함이 너무 큼. **frontmatter로 명시 입력**받도록 확장한다.

### 목표 포맷

```markdown
---
title: "2026년 종사자 심리지원교육 실시 계획(안)"
title_line1: "경상남도 노인맞춤돌봄서비스 광역지원기관"
subtitle: "- 마음 채움, 숲으로의 초대 -"
purpose: "노인맞춤돌봄 종사자의 심리·정서적 소진을 예방하고 자연 치유를 통한 회복과 재충전의 기회를 제공하여 종사자 심리건강 증진 및 돌봄 서비스 품질 향상을 도모하고자 교육을 진행함."
---

## Ⅰ. 관련 근거 및 필요성
- (재)경상남도사회서비스원 노인맞춤돌봄서비스-327(2025. 3. 10.)호
...
```

### 구현 순서

#### (1) `_parse_frontmatter()` 의 정규식 보강 — `dynamic_builder.py:247`

현재 정규식은 `[^"\']*` 로 값 안의 한글 콤마/공백은 통과하지만, 따옴표 미사용 + 값에 콜론(:)이 들어가면 깨질 수 있음. 안정성을 위해 다음으로 교체:

```python
m = re.match(r'^([A-Za-z_][A-Za-z0-9_]*)\s*:\s*(.*?)\s*$', line.rstrip())
if m:
    key = m.group(1)
    val = m.group(2)
    # 따옴표 제거
    if (val.startswith('"') and val.endswith('"')) or \
       (val.startswith("'") and val.endswith("'")):
        val = val[1:-1]
    config[key] = val
```

#### (2) `ParsedDocument` 데이터 클래스에 필드 추가 — `dynamic_builder.py:118-122`

```python
@dataclass
class ParsedDocument:
    title: str = ""
    title_line1: str = ""       # 추가: 표지 윗줄(기관/소속)
    subtitle: str = ""
    purpose: str = ""
    sections: list[Section] = field(default_factory=list)
```

#### (3) `parse_markdown_sections()` 에서 frontmatter 키 → doc 필드로 매핑 — `dynamic_builder.py:266-274` 근처

`prefix_overrides` 채우는 블록 바로 다음에 추가:
```python
if fm_config.get("title"):
    doc.title = fm_config["title"]
if fm_config.get("title_line1"):
    doc.title_line1 = fm_config["title_line1"]
if fm_config.get("subtitle"):
    doc.subtitle = fm_config["subtitle"]
if fm_config.get("purpose"):
    doc.purpose = fm_config["purpose"]
```

#### (4) 본문 패턴 매칭 → frontmatter 우선

- `# ` 라인은 `doc.title`이 frontmatter로 이미 채워져 있으면 **무시**(또는 덮어쓰지 않음).
- 부제/목적 자동 감지 블록(`dynamic_builder.py:291-299`)도 같은 식으로 가드: frontmatter로 이미 설정됐으면 스킵.
- 즉 frontmatter가 있으면 이를 우선, 없으면 기존 자동 감지 로직 유지(하위 호환).

#### (5) `replace_cover_text()` 가 2줄 제목을 처리하도록 확장 — `dynamic_builder.py:867-879`

현재는 `"기본 보고서 양식"` 한 곳만 치환. 표지 템플릿에 윗줄/아랫줄 자리가 어디인지 먼저 확인 필요.

작업 단계:
1. `assets/report-template.hwpx` 의 `section0.xml` part0(표지) 영역에서 "브라더 공기관" 위/아래에 텍스트 슬롯이 어떻게 배치돼 있는지 그레프로 확인.
2. 윗줄 슬롯이 별도로 존재하면 `doc.title_line1` 을 거기에 치환.
3. 별도 슬롯이 없다면, 메인 제목의 텍스트 노드를 `title_line1 + "\n" + title` 형태로 합성하거나, 메인 제목 `<hp:p>` 노드를 deepcopy하여 위에 한 줄 더 추가.
4. `title_line1` 이 빈 문자열이면 기존과 동일(회귀 없음).

**경계 조건**: 구조 분석 결과 슬롯이 없거나 모호하면 **여기서 중단하고 보고할 것.** Claude.ai에서 방향 재검토.

#### (6) `_build_purpose_element()` 호출 위치 확인 — `dynamic_builder.py:723-725`

목적 블록은 현재 `build_part2()` 본문 첫 번째 노드로 들어감(제목 블록 다음). 이는 표지가 아닌 본문 1페이지 상단에 박스가 위치한다는 뜻 — PDF 형태와 일치. **위치 변경 없음.**

단, `doc.purpose`가 frontmatter로 채워졌을 때도 정상 작동하는지 한 번 더 확인.

### 검증
1. `examples/external_report_sample.md` 신규 생성. 위 frontmatter + 9개 섹션(Ⅰ~Ⅸ)만 1~2개 항목씩 채워 PDF와 유사 구조로 작성.
2. 변환 명령:
   ```
   python cli.py -i examples/external_report_sample.md -o /tmp/out.hwpx --style 외부 --org "경상남도사회서비스원"
   ```
3. 결과 hwpx 확인:
   - 표지 윗줄 = "경상남도 노인맞춤돌봄서비스 광역지원기관"
   - 표지 메인 = "2026년 종사자 심리지원교육 실시 계획(안)"
   - 표지 부제 = "- 마음 채움, 숲으로의 초대 -"
   - 본문 1페이지 상단 박스 = 목적 텍스트
   - 섹션바 좌측 Ⅰ, 우측 "관련 근거 및 필요성" (6-2 결과 적용)
4. **회귀**: frontmatter가 전혀 없는 기존 `2026_상반기_업무추진계획.md` 도 정상 변환되는지 확인.

---

## 작업 6-6 — CLAUDE.md 작성 규칙 갱신 + 회귀 검증

### 갱신 항목 (사용자 승인 후 반영)

`CLAUDE.md` 의 "외부 스타일 마크다운 작성 규칙" 섹션에 다음을 추가:

```markdown
### 표지 입력 (frontmatter 권장)
마크다운 최상단에 작성:
---
title: "메인 제목"
title_line1: "기관/소속 (선택, 표지 상단 작은 줄)"
subtitle: "- 부제 -"
purpose: "목적 박스에 들어갈 자유 텍스트"
---

본문 자동 감지(frontmatter 미사용 시):
- 첫 `# 제목` 한 줄 → 메인 제목
- 그 다음 `-`로 시작하지 않는 줄 → 부제
- `목적:` 접두어 → 목적 박스

frontmatter 사용 시 본문 자동 감지보다 우선됩니다.

### 섹션 제목 작성
`## Ⅰ. 추진 배경` 처럼 로마숫자를 직접 적어도 자동 제거됩니다.
권장: `## 추진 배경` (로마숫자는 섹션바 좌측 셀에 자동 부여)
```

### 회귀 검증 일괄

| 입력 | 기대 결과 |
|------|----------|
| `2026_상반기_업무추진계획.md` (기존) | 회귀 없음 |
| `test_3depth.md` (3단계 들여쓰기) | 6-1 적용 후 □/○/― 정확 |
| `test_frontmatter.md` (frontmatter) | 회귀 없음 |
| `examples/external_report_sample.md` (신규) | 표지 2줄 + 부제 + 목적 박스 + Ⅰ~Ⅸ 섹션바 정상 |
| `tests/regression_indent_reset.md` (신규, 6-1 검증용) | □/○/― + □ 복귀 정확 |

---

## 일정 및 보고

| 단계 | 완료 시점 | 보고 방식 |
|------|----------|-----------|
| 6-1 ~ 6-4 묶음 완료 | 1차 보고 | 변경 라인 요약 + 회귀 케이스 결과 |
| 6-5 완료 | 2차 보고 | 신규 sample.md + 변환된 hwpx 스크린샷(또는 ZIP 내부 xml 발췌) |
| 6-6 진행 전 | 사용자 승인 대기 | CLAUDE.md diff 미리보기 제시 |

---

## 작업 범위 외 — 후속 작업지시서 #007 (예고)

이번 작업에서 **건드리지 않음**. PDF 5페이지 예산표처럼 다음 요건이 필요한 표는 다음 지시서에서 처리:
- 다단(2행 이상) 헤더
- 세로/가로 병합 셀 (`colSpan`, `rowSpan`)
- 셀 내 줄바꿈 / 셀 내 불릿 리스트
- 입력 포맷 옵션: 마크다운 셀에 `<br>` 허용, 또는 fenced block(```table```)으로 별도 표 정의 검토

---

## 주의사항

- 각 작업 6-x 완료 시 변경된 함수명 + 라인 범위만 짧게 보고. 자세한 diff는 사용자가 요청할 때만.
- 표지 템플릿 슬롯 구조(6-5 단계 5)가 모호하면 **즉시 멈추고 보고**. Claude.ai 방향 재검토 필요.
- frontmatter 값에 큰따옴표가 포함된 경우(예: `"- "특별 케이스" -"`)는 이번 범위 외. 단순 문자열만 지원.
- 기존 hwpx_generator(`--style 기존`) 경로에는 손대지 않음.
- 6-5에서 `replace_cover_text()` 의 `"브라더 공기관"`, `"2024. 5. 23."` 치환 로직은 그대로 유지(회귀 방지).
- 작업 중 별도 코드 추가/리팩터링 욕구가 생겨도 이번 범위에 한정. 추가 개선이 필요해 보이면 메모만 남기고 보고에 포함.
